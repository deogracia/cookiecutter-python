"""{{cookiecutter.friendly_name}}."""


def add_one(number: int) -> int:
    """Add one to 'number'.

    Args:
        number (int): integer to add one to.

    Returns:
        integer
    """
    return number + 1
